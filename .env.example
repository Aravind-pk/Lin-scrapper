# Copy to .env if you want to override a default. Never commit .env.
#
# No credentials live here. The service holds no LinkedIn session — every
# request carries the caller's own cookie header — so there is nothing to
# configure to make it work.

REQUEST_TIMEOUT=15.0
LOG_LEVEL=INFO
